server {
       listen 80;

       server_name 3.109.5.94:8000;

       root /var/www/static-site-nginx;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}