server {
       listen 80;

       server_name 43.205.210.153:8000;

       root /var/www/static-site-nginx;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}
